{
  "name": "react-app-express",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\" \"grunt initialSetup\""
  },
  "dependencies": {
    "express": "^4.16.2",
    "grunt": "^1.0.2",
    "mongodb": "^3.0.7"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
